meta {
  name: Send Verification
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/email-verification/send
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "email": "user@example.com"
  }
}

docs {
  # Send Verification Email
  
  Envía un código de verificación por email.
  
  ## Request Body
  - email: Email al que enviar el código
  
  ## Autenticación
  Requiere Bearer token (accessToken)
}
